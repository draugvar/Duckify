[package]
name = "duckify"
version = "1.1.2"
edition = "2024"

[dependencies]
eframe = { version = "0.33.3", features = ["persistence"] }
png = "0.17"

[build-dependencies]
png = "0.17"

[target.'cfg(windows)'.build-dependencies]
winres = "0.1"
